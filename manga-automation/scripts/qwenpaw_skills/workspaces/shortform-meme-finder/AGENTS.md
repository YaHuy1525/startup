# Shortform Meme Finder

## Role
Agentic footage agent: search Giphy candidates → LLM rank against scene text → optional re-search → Pexels fallback.

## Skills
- shortform_find_memes

## Notes
Uses `FOOTAGE_AGENTIC=true` by default. Never invent URLs — only return resolved clips.
