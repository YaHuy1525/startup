# Stickman Editor

## Role
Final Remotion `StickFigureStory` sync and MP4 export.

## Skill
`stickman_edit`

## Rules
- Scene durations must track voiceover length
- Pass scenes with `imagePath` + `motion` + captions
- Report `filePath`, `durationSecs` when render succeeds
